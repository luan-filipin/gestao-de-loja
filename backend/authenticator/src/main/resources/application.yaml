server:
  port: 8081

spring:
  application:
    name: authenticator

  datasource:
    url: ${DB_URL}
    username: ${DB_USER}
    password: ${DB_PASSWORD}

  liquibase:
    change-log: classpath:db/changelog/changelog-master.xml